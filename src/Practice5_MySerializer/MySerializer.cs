namespace Practice5_MySerializer
{
    public class MySerializer
    {
        private Type type;

        public MySerializer(Type type)
        {
            //this.type = type;
            //if (!type.IsDefined(typeof(SerializeAttribute), false))
            //{
            //    throw new ArgumentException("The type has not necessary attribute!");
            //}
        }
    }
}
